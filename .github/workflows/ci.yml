name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: python -m pip install --upgrade pip && python -m pip install -r requirements.txt -r requirements-security.txt

      - name: Verify dependency compatibility
        run: python -m pip check

      - name: Audit dependency vulnerabilities
        run: python -m pip_audit -r requirements.txt --strict

      - name: Run static quality checks
        run: python -m ruff check .

      - name: Verify local-first privacy controls
        run: python scripts/security_review.py --root .

      - name: Run automated tests
        run: python -m pytest -q

      - name: Publish readiness evidence
        run: python scripts/release_readiness.py --root .
