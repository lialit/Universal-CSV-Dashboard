# Pull Request Labeler Regression Marker

This temporary repository-automation marker validates that changes under `.github/` receive the `area: github` label. Because it is also Markdown, it should receive `area: docs`.
